export function faccat15() {
    alert("Verificação de valor positivo ou negativo");

    let valor = parseFloat(prompt("Digite um valor:"));

    if (valor >= 0) { alert("É um valor positivo."); }
    else { alert("É um valor negativo."); }
}