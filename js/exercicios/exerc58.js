export function faccat58(){
    alert("Impressão de Números de 1 a 10 em Ordem Decrescente (while)");
    let contador = 10;

    while (contador >= 1) {
      alert(contador);
      contador--;
    }
}